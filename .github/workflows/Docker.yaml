name: Docker
on: [workflow_dispatch]

jobs:
  docker-job:
    runs-on: ubuntu-latest
    container:
      image: dhi.io/node:25-debian13-sfw-ent-dev
    # credentials:
    # username:
    #password
      env:
        API_URL: some-url.com
        ports: 
          -80
      #volumes:
       # - vol-name:/path/in/container
       # - /pat/to/container
       # - /path/in/host:/path/in/container
      # options: --cpus 1
  steps:
   - name: Log Node & oS version
     run: |
        node -v
        cat /etc/os-release 
    - name: Log env
      run: echo $API_URL    
